diff --git a/code/machine/interrupt.cc b/code/machine/interrupt.cc
index 4ed8291..b20adb7 100644
--- a/code/machine/interrupt.cc
+++ b/code/machine/interrupt.cc
@@ -158,6 +158,7 @@ void Interrupt::OneTick() {
                                  // (interrupt handlers run with
                                  // interrupts disabled)
     CheckIfDue(FALSE);           // check for pending interrupts
+    kernel->scheduler->CheckSleepThread();
     ChangeLevel(IntOff, IntOn);  // re-enable interrupts
     if (yieldOnReturn) {         // if the timer device handler asked
                                  // for a context switch, ok to do it now
@@ -196,6 +197,7 @@ void Interrupt::YieldOnReturn() {
 //	more for us to do.
 //----------------------------------------------------------------------
 void Interrupt::Idle() {
+    kernel->scheduler->CheckSleepThread();
     DEBUG(dbgInt, "Machine idling; checking for interrupts.");
     status = IdleMode;
     if (CheckIfDue(TRUE)) {  // check for any pending interrupts
diff --git a/code/test/Makefile b/code/test/Makefile
index 5deab5e..9228874 100644
--- a/code/test/Makefile
+++ b/code/test/Makefile
@@ -112,7 +112,7 @@ ifeq ($(hosttype),unknown)
 PROGRAMS = unknownhost
 else
 # change this if you create a new test program!
-PROGRAMS = add abs halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite child exec test_sem sinhvien voinuoc main
+PROGRAMS = add abs sleep halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite child exec test_sem sinhvien voinuoc main
 endif
 
 all: $(PROGRAMS)
@@ -126,6 +126,13 @@ halt: halt.o start.o
 	$(LD) $(LDFLAGS) start.o halt.o -o halt.coff
 	$(COFF2NOFF) halt.coff halt
 
+sleep.o: sleep.c
+	$(CC) $(CFLAGS) -c sleep.c
+
+sleep: sleep.o start.o
+	$(LD) $(LDFLAGS) start.o sleep.o -o sleep.coff
+	$(COFF2NOFF) sleep.coff sleep
+
 add.o: add.c
 	$(CC) $(CFLAGS) -c add.c
 
diff --git a/code/threads/scheduler.cc b/code/threads/scheduler.cc
index ed111c4..d0af36a 100644
--- a/code/threads/scheduler.cc
+++ b/code/threads/scheduler.cc
@@ -39,6 +39,7 @@ int compare(Thread* x, Thread* y) {
 Scheduler::Scheduler() {
     //readyList = new List<Thread *>;
     readyList = new SortedList<Thread *>(&compare);
+    sleepList = new List<Thread*>();
     toBeDestroyed = NULL;
 }
 
@@ -173,3 +174,27 @@ void Scheduler::Print() {
     readyList->Apply(ThreadPrint);
 }
 
+
+void Scheduler::CheckSleepThread() {
+
+    ASSERT(kernel->interrupt->getLevel() == IntOff);
+
+    List<Thread*> *tmp = new List<Thread*>();
+
+    int now = kernel->stats->totalTicks;
+
+    while (!sleepList->IsEmpty()) {
+
+        Thread* t = sleepList->RemoveFront();
+
+        if (t->waketick <= now) {
+            ReadyToRun(t);
+        }
+        else {
+            tmp->Append(t);
+        }
+    }
+
+    while(!tmp->IsEmpty())
+        sleepList->Append(tmp->RemoveFront());
+}
diff --git a/code/threads/scheduler.h b/code/threads/scheduler.h
index 7151bd2..96ea41b 100644
--- a/code/threads/scheduler.h
+++ b/code/threads/scheduler.h
@@ -31,13 +31,15 @@ class Scheduler {
     void CheckToBeDestroyed();  // Check if thread that had been
                                 // running needs to be deleted
     void Print();               // Print contents of ready list
-
+    void CheckSleepThread();
+    List<Thread*>* sleepList;
     // SelfTest for scheduler is implemented in class Thread
 
    private:
     //List<Thread*>* readyList;  // queue of threads that are ready to run,
                                // but 
     SortedList<Thread*>* readyList;
+    //List<Thread*>* sleepList;
     Thread* toBeDestroyed;     // finishing thread to be destroyed
                                // by the next thread that runs
 };
diff --git a/code/threads/thread.cc b/code/threads/thread.cc
index fadcb38..8870bfa 100644
--- a/code/threads/thread.cc
+++ b/code/threads/thread.cc
@@ -39,6 +39,7 @@ Thread::Thread(char *threadName, bool _has_dynamic_name /*=false*/) {
     name = threadName;
     stackTop = NULL;
     stack = NULL;
+    waketick = 0;
     status = JUST_CREATED;
     for (int i = 0; i < MachineStateSize; i++) {
         machineState[i] = NULL;  // not strictly necessary, since
diff --git a/code/threads/thread.h b/code/threads/thread.h
index 82a27f1..2571836 100644
--- a/code/threads/thread.h
+++ b/code/threads/thread.h
@@ -90,6 +90,7 @@ class Thread {
     int processID;
     int parrentID;
     int priority;
+    int waketick;
     int exitStatus;
     void FreeSpace() {
         if (space != 0) delete space;
diff --git a/code/userprog/.syscall.h.swp b/code/userprog/.syscall.h.swp
deleted file mode 100644
index a6840e3..0000000
Binary files a/code/userprog/.syscall.h.swp and /dev/null differ
diff --git a/code/userprog/exception.cc b/code/userprog/exception.cc
index 8514f06..ae594bd 100644
--- a/code/userprog/exception.cc
+++ b/code/userprog/exception.cc
@@ -479,6 +479,16 @@ void ExceptionHandler(ExceptionType which) {
                     return handle_SC_Signal();
                 case SC_GetPid:
                     return handle_SC_GetPid();
+		case SC_Sleep:
+		{
+    		    int ticks = kernel->machine->ReadRegister(4);
+
+    		    int result = SysSleep(ticks);
+
+    		    kernel->machine->WriteRegister(2, result);
+
+    		    break;
+		}	
                 /**
                  * Handle all not implemented syscalls
                  * If you want to write a new handler for syscall:
@@ -486,7 +496,7 @@ void ExceptionHandler(ExceptionType which) {
                  * - Write handle_SC_name()
                  * - Add new case for SC_name
                  */
-                case SC_Create:
+		case SC_Create:
                 case SC_Remove:
                 case SC_ThreadFork:
                 case SC_ThreadYield:
@@ -504,5 +514,11 @@ void ExceptionHandler(ExceptionType which) {
             cerr << "Unexpected user mode exception" << (int)which << "\n";
             break;
     }
+    //kernel->machine->AdvancePC();
+    int pc = kernel->machine->ReadRegister(PCReg);
+
+    kernel->machine->WriteRegister(PrevPCReg, pc);
+    kernel->machine->WriteRegister(PCReg, pc + 4);
+    kernel->machine->WriteRegister(NextPCReg, pc + 8);
     ASSERTNOTREACHED();
 }
diff --git a/code/userprog/ksyscall.h b/code/userprog/ksyscall.h
index 4401314..8c32caf 100644
--- a/code/userprog/ksyscall.h
+++ b/code/userprog/ksyscall.h
@@ -22,6 +22,22 @@ void SysHalt() { kernel->interrupt->Halt(); }
 
 int SysAdd(int op1, int op2) { return op1 + op2; }
 int SysAbs(int op1) { return (op1 >0 ? op1 : -op1); }
+int SysSleep(int op1){
+
+    IntStatus oldLevel = kernel->interrupt->SetLevel(IntOff);
+
+    Thread* t = kernel->currentThread;
+
+    t->waketick = kernel->stats->totalTicks + op1;
+
+    kernel->scheduler->sleepList->Append(t);
+
+    t->Sleep(false);
+
+    kernel->interrupt->SetLevel(oldLevel);
+
+    return op1;
+}
 
 int SysReadNum() {
     readUntilBlank();
diff --git a/code/userprog/syscall.h b/code/userprog/syscall.h
index 6b97db7..525451f 100644
--- a/code/userprog/syscall.h
+++ b/code/userprog/syscall.h
@@ -49,7 +49,7 @@
 #define SC_Signal 53
 #define SC_GetPid 54
 #define SC_Abs 55
-
+#define SC_Sleep 56
 #ifndef IN_ASM
 
 /* The system call interface.  These are the operations the Nachos
@@ -89,6 +89,7 @@ void PrintString(char *buffer);
 
 /* This user program is done (status = 0 means exited normally). */
 void Exit(int status);
+void Sleep(int ticks);
 
 /* A unique identifier for an executing user program (address space) */
 typedef int SpaceId;
diff --git a/pandc.diff b/pandc.diff
index e69de29..50412c3 100644
--- a/pandc.diff
+++ b/pandc.diff
@@ -0,0 +1,253 @@
+diff --git a/code/lib/list.h b/code/lib/list.h
+index 3ce9553..6ab0013 100644
+--- a/code/lib/list.h
++++ b/code/lib/list.h
+@@ -100,6 +100,8 @@ class SortedList : public List<T> {
+     void SanityCheck() const;  // has this list been corrupted?
+     void SelfTest(T *p, int numEntries);
+     // verify module is working
++    // in a sorted list
++    void Append(T item) { Insert(item); }
+ 
+    private:
+     int (*compare)(T x, T y);  // function for sorting list elements
+@@ -108,7 +110,8 @@ class SortedList : public List<T> {
+         Insert(item);
+     }                                      // *pre*pending has no meaning
+                                            //	in a sorted list
+-    void Append(T item) { Insert(item); }  // neither does *ap*pend
++   // void Append(T item) { Insert(item); }  // neither does *ap*pend
++   
+ };
+ 
+ // The following class can be used to step through a list.
+diff --git a/code/machine/timer.cc b/code/machine/timer.cc
+index c18c317..a0736c3 100644
+--- a/code/machine/timer.cc
++++ b/code/machine/timer.cc
+@@ -64,7 +64,7 @@ void Timer::CallBack() {
+ //----------------------------------------------------------------------
+ 
+ void Timer::SetInterrupt() {
+-    if (!disable) {
++    if (!disable && false) {
+         int delay = TimerTicks;
+ 
+         if (randomize) {
+diff --git a/code/test/Makefile b/code/test/Makefile
+index f7b5063..5deab5e 100644
+--- a/code/test/Makefile
++++ b/code/test/Makefile
+@@ -112,7 +112,7 @@ ifeq ($(hosttype),unknown)
+ PROGRAMS = unknownhost
+ else
+ # change this if you create a new test program!
+-PROGRAMS = add abs halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite exec test_sem sinhvien voinuoc main
++PROGRAMS = add abs halt shell matmult sort segments test_syscall num_io char_io random str_io ascii bubblesort help create_file open_file readwrite child exec test_sem sinhvien voinuoc main
+ endif
+ 
+ all: $(PROGRAMS)
+@@ -230,6 +230,13 @@ readwrite: readwrite.o start.o
+ 	$(LD) $(LDFLAGS) start.o readwrite.o -o readwrite.coff
+ 	$(COFF2NOFF) readwrite.coff readwrite
+ 
++child.o: child.c
++	$(CC) $(CFLAGS) -c child.c
++
++child:child.o start.o
++	$(LD) $(LDFLAGS) start.o child.o -o child.coff
++	$(COFF2NOFF) child.coff child
++
+ exec.o: exec.c
+ 	$(CC) $(CFLAGS) -c exec.c
+ exec: exec.o start.o
+diff --git a/code/test/add.c b/code/test/add.c
+index 9bb9aa1..2d016cf 100644
+--- a/code/test/add.c
++++ b/code/test/add.c
+@@ -8,11 +8,14 @@
+ #include "syscall.h"
+ 
+ int main() {
+-    int result;
++    int result, i;
+ 
+     result = Add(40, 23);
+     //PrintNum(result);
+-
+-    Halt();
++    for(i=0; i<10; i++){
++	 Write("C\n", 2,1);
++     }
++    Exit(0);
++    //Halt();
+     /* not reached */
+ }
+diff --git a/code/test/child.c b/code/test/child.c
+new file mode 100644
+index 0000000..4e5be54
+--- /dev/null
++++ b/code/test/child.c
+@@ -0,0 +1,10 @@
++#include "syscall.h"
++
++int main(){
++	int i;
++	for(i =0; i<10; i++){
++		Write("Child is rinning\n", 17,1);
++	}
++
++	Halt();
++}
+diff --git a/code/test/exec.c b/code/test/exec.c
+index feadac1..717e866 100644
+--- a/code/test/exec.c
++++ b/code/test/exec.c
+@@ -8,10 +8,18 @@
+ 
+ int main() {
+     int pid;
+-    pid = Exec("num_io");
+-    if (pid < 0) {
+-        Write("Exec failed: ", 14, stdout);
+-        PrintNum(pid);
+-    } else
++    int i;
++
++    pid = Exec("../test/add");
++
++    for(i=0;i<10;i++){
++    	Write("P\n", 2,stdout);
++    }
++    if (pid>=0) {
+         Join(pid);
++    } else{
++        Write("Exec failed\n",12,stdout);
++    }
++    Exit(0);
++    //Halt();
+ }
+diff --git a/code/threads/pcb.cc b/code/threads/pcb.cc
+index a740854..065e717 100644
+--- a/code/threads/pcb.cc
++++ b/code/threads/pcb.cc
+@@ -70,6 +70,7 @@ int PCB::Exec(char* filename, int id) {
+     // nên khi hàm này kết thúc thì giá trị của biến này cũng bị xóa
+     // Đừng hỏi tôi đã mất bao lâu để nhận ra điều này :)
+     this->thread->Fork(StartProcess_2, &this->thread->processID);
++    kernel->currentThread->Yield();
+ 
+     multex->V();
+     // Trả về id.
+diff --git a/code/threads/scheduler.cc b/code/threads/scheduler.cc
+index 1d52916..ed111c4 100644
+--- a/code/threads/scheduler.cc
++++ b/code/threads/scheduler.cc
+@@ -22,7 +22,14 @@
+ #include "debug.h"
+ #include "scheduler.h"
+ #include "main.h"
+-
++int compare(Thread* x, Thread* y) {
++    if (x->priority > y->priority)
++        return 1;
++    else if (x->priority < y->priority)
++        return -1;
++    else
++        return 0;
++}
+ //----------------------------------------------------------------------
+ // Scheduler::Scheduler
+ // 	Initialize the list of ready but not running threads.
+@@ -30,7 +37,8 @@
+ //----------------------------------------------------------------------
+ 
+ Scheduler::Scheduler() {
+-    readyList = new List<Thread *>;
++    //readyList = new List<Thread *>;
++    readyList = new SortedList<Thread *>(&compare);
+     toBeDestroyed = NULL;
+ }
+ 
+@@ -54,7 +62,7 @@ void Scheduler::ReadyToRun(Thread *thread) {
+     DEBUG(dbgThread, "Putting thread on ready list: " << thread->getName());
+ 
+     thread->setStatus(READY);
+-    readyList->Append(thread);
++    readyList->Append(thread);	
+ }
+ 
+ //----------------------------------------------------------------------
+@@ -164,3 +172,4 @@ void Scheduler::Print() {
+     cout << "Ready list contents:\n";
+     readyList->Apply(ThreadPrint);
+ }
++
+diff --git a/code/threads/scheduler.h b/code/threads/scheduler.h
+index a96f228..7151bd2 100644
+--- a/code/threads/scheduler.h
++++ b/code/threads/scheduler.h
+@@ -35,8 +35,9 @@ class Scheduler {
+     // SelfTest for scheduler is implemented in class Thread
+ 
+    private:
+-    List<Thread*>* readyList;  // queue of threads that are ready to run,
+-                               // but not running
++    //List<Thread*>* readyList;  // queue of threads that are ready to run,
++                               // but 
++    SortedList<Thread*>* readyList;
+     Thread* toBeDestroyed;     // finishing thread to be destroyed
+                                // by the next thread that runs
+ };
+diff --git a/code/threads/thread.cc b/code/threads/thread.cc
+index be9f7d6..fadcb38 100644
+--- a/code/threads/thread.cc
++++ b/code/threads/thread.cc
+@@ -21,6 +21,7 @@
+ #include "switch.h"
+ #include "synch.h"
+ #include "sysdep.h"
++#include <stdlib.h>
+ 
+ // this is put at the top of the execution stack, for detecting stack overflows
+ const int STACK_FENCEPOST = 0xdedbeef;
+@@ -45,6 +46,8 @@ Thread::Thread(char *threadName, bool _has_dynamic_name /*=false*/) {
+                                  // of machine registers
+     }
+     space = NULL;
++    priority = rand()%10;
++    printf("priority: %d, name: %s \n", priority, name);
+ }
+ 
+ //----------------------------------------------------------------------
+@@ -100,7 +103,10 @@ void Thread::Fork(VoidFunctionPtr func, void *arg) {
+ 
+     oldLevel = interrupt->SetLevel(IntOff);
+     scheduler->ReadyToRun(this);  // ReadyToRun assumes that interrupts
+-                                  // are disabled!
++                                  // are disab
++    if (this->priority > kernel->currentThread->priority){
++	    kernel->currentThread->Yield();
++    }
+     (void)interrupt->SetLevel(oldLevel);
+ }
+ 
+diff --git a/code/threads/thread.h b/code/threads/thread.h
+index 3fbf156..82a27f1 100644
+--- a/code/threads/thread.h
++++ b/code/threads/thread.h
+@@ -89,6 +89,7 @@ class Thread {
+ 
+     int processID;
+     int parrentID;
++    int priority;
+     int exitStatus;
+     void FreeSpace() {
+         if (space != 0) delete space;
+diff --git a/code/userprog/.syscall.h.swp b/code/userprog/.syscall.h.swp
+new file mode 100644
+index 0000000..a6840e3
+Binary files /dev/null and b/code/userprog/.syscall.h.swp differ
+diff --git a/pandc.diff b/pandc.diff
+new file mode 100644
+index 0000000..e69de29
